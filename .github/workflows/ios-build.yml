name: "MIMO_NDI iOS Cloud Build"

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: "Checkout Code"
        uses: actions/checkout@v4
        with:
          lfs: false

      - name: "Installer Flutter"
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'

      - name: "Télécharger le SDK NDI iOS depuis ma Release"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p ios/NDISDK/lib
          
          echo "Téléchargement de libndi_ios.zip depuis GitHub Release..."
          gh release download v1.0.0-sdk --repo ${{ github.repository }} --pattern 'libndi_ios.zip'
          echo "Décompression de la librairie..."
          unzip -o libndi_ios.zip -d ios/NDISDK/lib/
          
          echo "Téléchargement des Headers NDI..."
          mkdir -p ios/NDISDK/include
          gh release download v1.0.0-sdk --repo ${{ github.repository }} --pattern 'include.zip'
          unzip -o include.zip -d ios/NDISDK/include/
          
          echo "Vérification de la présence du fichier :"
          ls -lh ios/NDISDK/lib/libndi_ios.a || (echo "ERREUR : Fichier libndi_ios.a manquant !" && exit 1)

      - name: "Installer les dépendances Flutter"
        run: |
          flutter config --enable-ios
          # On ne fait SURTOUT PAS 'git checkout ios' car cela écraserait ta photo et tes réglages audio !
          flutter pub get
          
          # Tentative de génération d'icônes et splash (silencieux si ça échoue)
          dart run flutter_launcher_icons:main || echo "Icon generation failed, using defaults"
          dart run flutter_native_splash:create || echo "Splash generation failed, using defaults"

      - name: "Configurer Xcode pour NDI"
        run: |
          sudo gem install xcodeproj
          cd ios
          # Ce script ajoute les frameworks et lie la libndi_ios.a au projet Runner
          ruby configure_xcode.rb

      - name: "Installation CocoaPods"
        run: |
          cd ios
          pod install --repo-update

      - name: "Générer l'IPA (Non signé)"
        run: |
          # Construction du package iOS en mode Release
          flutter build ios --release --no-codesign --verbose
          
          # Préparation de l'archive IPA pour le téléchargement
          cd build/ios/iphoneos
          mkdir -p Payload
          cp -r Runner.app Payload/
          zip -r MIMO_NDI.ipa Payload

      - name: "Exporter l'IPA (.ipa)"
        uses: actions/upload-artifact@v4
        with:
          name: mimo-ndi-ipa
          path: build/ios/iphoneos/MIMO_NDI.ipa
